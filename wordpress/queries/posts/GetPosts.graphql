query GetAllPosts($first: Int!, $after: String, $where: RootQueryToPostConnectionWhereArgs) {
    categories {
        nodes {
            name
            slug
        }
    }
    tags {
        nodes {
            name
            slug
        }
    }
    posts(
        first: $first
        after: $after
        where: $where
    ) {
        pageInfo {
            hasNextPage,
            endCursor
        }
        nodes {
           ...PostCardFields,
            status
        }
    }
}
